import React from 'react';\nimport { Check } from 'lucide-react';\n\nconst StepIndicator = ({ \n  steps = [],\n  currentStep = 0,\n  className = \"\"\n}) => {\n  return (\n    <div className={`flex items-center justify-center space-x-8 ${className}`}>\n      {steps.map((step, index) => {\n        const isCompleted = index < currentStep;\n        const isActive = index === currentStep;\n        const isUpcoming = index > currentStep;\n        \n        return (\n          <div key={step.id || index} className=\"flex items-center\">\n            <div className=\"flex items-center\">\n              {/* Step Circle */}\n              <div className={`\n                flex items-center justify-center w-8 h-8 rounded-full border-2 text-sm font-semibold transition-all duration-200\n                ${isCompleted \n                  ? 'bg-green-500 border-green-500 text-white' \n                  : isActive \n                    ? 'bg-visa-orange-500 border-visa-orange-500 text-white'\n                    : 'bg-gray-100 border-gray-300 text-gray-500'\n                }\n              `}>\n                {isCompleted ? (\n                  <Check className=\"w-4 h-4\" />\n                ) : (\n                  <span>{index + 1}</span>\n                )}\n              </div>\n              \n              {/* Step Label */}\n              <span className={`ml-3 text-sm font-medium transition-colors duration-200 ${\n                isCompleted \n                  ? 'text-green-600' \n                  : isActive \n                    ? 'text-visa-orange-500'\n                    : 'text-gray-500'\n              }`}>\n                {step.title || step.name || step}\n              </span>\n            </div>\n            \n            {/* Connector Line */}\n            {index < steps.length - 1 && (\n              <div className={`ml-8 w-16 h-0.5 transition-colors duration-200 ${\n                index < currentStep ? 'bg-green-500' : 'bg-gray-300'\n              }`} />\n            )}\n          </div>\n        );\n      })}\n    </div>\n  );\n};\n\n// Predefined step configurations for common flows\nexport const visaApplicationSteps = [\n  { id: 'details', title: 'Traveler and Contact Details' },\n  { id: 'payment', title: 'Make Payment' },\n  { id: 'confirmation', title: 'Confirmation' }\n];\n\nexport const visaProcessingSteps = [\n  { id: 'submitted', title: 'Application Submitted' },\n  { id: 'review', title: 'Under Review' },\n  { id: 'processing', title: 'Processing' },\n  { id: 'ready', title: 'Ready for Collection' }\n];\n\nexport default StepIndicator;"